import React from 'react'
import TransactionManager from './TransactionManager';

const GestionBarajas: React.FC = () => {
    return <TransactionManager filterBarajas={false} />;
  };

export default GestionBarajas;