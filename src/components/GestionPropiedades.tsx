import React from 'react'
import TransactionManager from './TransactionManager';

const GestionPropiedades: React.FC = () => {
    return <TransactionManager filterBarajas={true} />;
  };

export default GestionPropiedades;